## 1. 单页壳与单入口搭建

- [x] 1.1 合并菜单页与比赛页所需的 HTML 结构，建立单页壳并保留 `curtain`、菜单 canvas、比赛 canvas、HUD、摇杆和完赛面板容器
- [x] 1.2 调整 `webpack.config.js` 为单入口应用输出，并将 `src/index.ts` / `src/game.ts` 的启动职责收敛到新的应用入口
- [x] 1.3 新建应用壳控制器，负责视图容器显隐、全局 DOM 引用初始化和唯一动画循环的调度

## 2. 页内路由接入

- [x] 2.1 设计并实现 `#/menu` 与 `#/race?speeder=...` 的 hash 路由解析与导航工具
- [x] 2.2 将菜单进入比赛的流程改为由应用壳驱动路由切换，而不是通过 `window.location.href` 跳转 HTML
- [x] 2.3 接入浏览器前进后退行为，验证从比赛返回菜单时仍停留在单页壳内

## 3. 菜单场景生命周期改造

- [x] 3.1 为 `MenuScene` 增加显式生命周期边界，拆分事件绑定、激活、停用和销毁逻辑
- [x] 3.2 将 `PLAY` 转场改为在完成后触发 `onPlay(speederIndex)` 回调给应用壳
- [x] 3.3 验证多次进入菜单后菜单输入事件不会重复触发，标题与按钮动画保持正常

## 4. 比赛场景与比赛会话重入改造

- [x] 4.1 为 `GameScene` 增加可重复进入的会话初始化、重置和销毁能力，并移除对页面初次加载语义的依赖
- [x] 4.2 将比赛入口改为由应用壳直接传入 `speederIndex` 与所需 DOM/控制对象，而不是从 URL 解析页面参数
- [x] 4.3 为比赛会话建立“返回菜单后重新开始”和“完赛后再次开始”的干净重入流程，确保倒计时、计时、圈数、完赛面板和玩家/CPU 状态会正确重置

## 5. 黑幕与 HUD 所属权收口

- [x] 5.1 盘点 `MenuScene`、`GameScene`、`Vehicle` 中对 `curtain` 和比赛 HUD 的直接 DOM 访问点
- [x] 5.2 将视图级 HUD 显隐和全局 `curtain` 初始状态上收至应用壳或受控 UI 层
- [x] 5.3 保持掉出赛道恢复和完赛黑幕流程在单页模式下继续可用，并验证不会与菜单转场抢占状态

## 6. 回归验证与收尾

- [x] 6.1 本地验证菜单初始加载、菜单进入比赛、比赛返回菜单、再次开始比赛、完赛后二次进入比赛的完整闭环
- [x] 6.2 本地验证掉出赛道恢复、倒计时、HUD、摇杆显示与浏览器返回行为在单页模式下都正常
- [x] 6.3 运行构建并清理旧双页面入口残留，确认最终交付结构与 OpenSpec 方案一致
