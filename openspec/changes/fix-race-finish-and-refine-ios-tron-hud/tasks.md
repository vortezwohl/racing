## 1. 完赛触发修复

- [x] 1.1 梳理玩家过线、加圈与完赛触发的现有链路，明确 `Vehicle` 与 `GameScene` 之间新的完赛事件入口
- [x] 1.2 实现玩家完成最后一圈时的显式完赛通知，并让 `GameScene` 在该通知内直接触发结果面板
- [x] 1.3 为 `handleRaceFinish()` 增加幂等保护，避免重复结算、重复音效与重复状态写入
- [ ] 1.4 回归验证玩家完成两圈后，结算面板能够稳定弹出，且不会出现“已完赛但无面板”的状态

## 2. HUD 极简视觉重构

- [x] 2.1 重构 `RaceHud` 顶栏绘制，移除厚重大框与装饰性面板，改为轻量排版驱动的左上/中上/右上信息布局
- [x] 2.2 调整 HUD 字体、字号、字重、颜色与辉光，使其符合白字轻蓝光的简约现代风格
- [x] 2.3 优化桌面与窄屏视口下的 HUD 间距和对齐，确保布局稳定且不互相遮挡

## 3. 设置按钮与弹窗交互优化

- [x] 3.1 将右上角设置入口改为无外框齿轮图标，并加入短时按压反馈状态
- [x] 3.2 在设置弹窗中新增 `RESUME` 操作，并保持 `RESTART RACE` 与 `EXIT TO MENU`
- [x] 3.3 接通 `RESUME` 关闭弹窗、`RESTART RACE` 重建新比赛会话、`EXIT TO MENU` 返回菜单的交互路径
- [ ] 3.4 验证设置弹窗打开期间比赛不会暂停，且 `RESUME` 能无残留地回到赛中 HUD

## 4. 返回菜单转场统一

- [x] 4.1 梳理设置弹窗 `EXIT TO MENU`、结算面板 `BACK` 以及其它比赛内返回菜单入口，统一收口到 `AppShell` 转场接口
- [x] 4.2 复用菜单 `PLAY` 的黑幕帘风格，实现“先渐黑、黑屏内切回菜单、再从黑幕渐显菜单”的统一返回流程
- [ ] 4.3 验证不同返回菜单入口都不会直接闪切到菜单，且黑幕期间不会出现错误中间帧

## 5. 结算面板视觉统一

- [x] 5.1 将结果面板视觉重构为更轻的半透明玻璃卡片风格，统一到新的极简 HUD 语言
- [x] 5.2 调整结算页文字层级与排行榜排版，减少赛博装饰感，保留必要的 Tron 色彩
- [ ] 5.3 验证赛后 `BACK`、`RETRY`、齿轮按钮和排行榜更新在新视觉下仍保持可点击与可读

## 6. 验证与收尾

- [x] 6.1 本地运行构建，确认本次修复未引入 TypeScript 或打包错误
- [ ] 6.2 手工验证菜单进比赛、两圈完赛、设置弹窗、`RESUME`、退出菜单黑幕帘、赛后 `BACK / RETRY` 与重新随机发车位流程
