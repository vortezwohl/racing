## 1. 比赛 HUD 基础设施

- [x] 1.1 在比赛页壳体中新增专用 HUD Canvas 容器，并整理与 `game` canvas、`marker-layer`、`joystick`、`curtain` 的层级关系
- [x] 1.2 在 `GameScene` 中建立统一的比赛 UI 状态对象，收口圈数、名次、计时、倒计时、时速、设置弹窗和结算面板状态
- [x] 1.3 移除或替换 `Track.ts`、`Vehicle.ts`、`GameScene.ts` 中对旧 HUD 文本 DOM 的直接写入路径，使比赛 HUD 改由状态驱动

## 2. Canvas HUD 绘制与布局

- [x] 2.1 实现 Tron 风格 HUD 绘制器，统一渲染左上圈数/名次、中上计时、右上 `KM/H` 与起跑前倒计时
- [x] 2.2 接入实时数据刷新逻辑，使圈数、名次、计时和时速在比赛中持续更新
- [x] 2.3 适配窗口尺寸变化，验证 HUD 在桌面和移动端窄屏视口下仍保持稳定布局与可读性

## 3. 设置按钮与会话重开流程

- [x] 3.1 在 HUD Canvas 中实现右上角齿轮按钮的绘制与热区命中检测
- [x] 3.2 实现非暂停式设置弹窗，并接入“重新开始比赛”和“退出到菜单”两个操作
- [x] 3.3 将设置弹窗中的重开与退出统一接到 `AppShell` 的比赛会话重建 / 菜单返回路径，确保不会复用旧比赛会话
- [x] 3.4 验证通过设置弹窗重开比赛后，玩家与 NPC 的发车位会重新随机分配

## 4. 结算面板与排行榜

- [x] 4.1 用半透明结果面板替换旧黑场结算页，保留赛道背景并在结算状态下维持齿轮按钮位置不变
- [x] 4.2 记录并显示玩家最终名次、平均时速、总成绩时间与每圈耗时
- [x] 4.3 构建本场排行榜数据结构，按名字与颜色渲染玩家和 NPC 条目
- [x] 4.4 让结果面板在玩家完赛后继续接收 NPC 状态更新，并刷新排行榜显示
- [x] 4.5 在结果面板底部实现 `BACK` 与 `RETRY` 热区操作，并验证 `RETRY` 同样会重新随机发车位

## 5. 回归验证与清理

- [x] 5.1 清理旧 `dashboard` / `finish-screen` 的主显示职责，仅保留当前任务仍需要的最小兼容结构
- [x] 5.2 本地运行构建，确认比赛内 UI 改造未引入 TypeScript 或打包错误
- [ ] 5.3 手工验证菜单进比赛、赛中打开设置、退出到菜单、赛后结算、`BACK`、`RETRY`、移动端摇杆层级与 NPC 赛后更新流程
