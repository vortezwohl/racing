## 1. 参赛名单生成收口

- [x] 1.1 将比赛参赛名单来源统一为 `menuVehicles`，让比赛初始化先生成 1 名玩家和目标 4 名 NPC 的候选名单。
- [x] 1.2 实现规则 B 的 NPC 补位逻辑：先优先选择未使用且不同于玩家的唯一车型，不足时再允许重复车型补足到 4 名 NPC。
- [x] 1.3 验证任意玩家选车下，比赛初始化后的总参赛数恒为 5。

## 2. 起跑网格与随机车位

- [x] 2.1 基于 `track.startPoint`、`track.startDirection` 和车辆最大尺寸生成固定的 `2 前 3 后` 交错起跑槽位。
- [x] 2.2 为 5 名参赛者实现起跑槽位随机分配，并确保每名参赛者只占用 1 个槽位。
- [x] 2.3 调整玩家与 NPC 的创建流程，使它们都使用统一的已分配槽位位置、方向和旋转完成出生。

## 3. 起跑安全间距验证

- [x] 3.1 为起跑槽位计算加入基于最大 `width` / `length` 的安全边距，避免初始重叠。
- [x] 3.2 检查并修正 `raceVehicleStates`、NPC 身份信息和后续比赛逻辑对新增 5 人阵容的适配，确保不会因为人数变化漏掉状态初始化。
- [x] 3.3 运行本地构建，确认比赛初始化改动未引入 TypeScript 或 bundling 回归。

## 4. 手工回归验证

- [ ] 4.1 手工验证玩家选择任意车型进入比赛时，开场总能看到 5 台载具参赛。
- [ ] 4.2 手工验证 5 台载具在起跑线处呈 `2 前 3 后` 交错排布，且每次开赛车位分配会变化。
- [ ] 4.3 手工验证开赛瞬间车辆不会互相嵌入或因初始重叠触发异常碰撞表现。
